# Normalise line endings so the repo stays consistent across platforms.
* text=auto eol=lf
*.png binary
*.jpg binary

# npm writes this file; counting it as source makes the language bar claim
# the project is a fifth JavaScript when it contains no JavaScript at all.
package-lock.json linguist-generated=true
